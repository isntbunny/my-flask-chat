name: Keep Render Alive

on:
  schedule:
    # 每 10 分钟运行一次 (UTC 时间)
    - cron: '*/10 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl the endpoint
        run: curl -s https://isntbunny.onrender.com > /dev/null
